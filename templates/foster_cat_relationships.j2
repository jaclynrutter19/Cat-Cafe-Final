<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="static/style.css">
    <title>Cat Cafe</title>
</head>
<body>
    <header>
        <p class="company"><a href="/">Cat Cafe</a></p>
        <nav>
            <a href="cats">Cats</a>
            <a href="cafe_products">Cafe Products</a>
            <a href="employees">Employees</a>
            <a href="customers">Customers</a>
            <a href="foster_parents">Foster Parents</a>
            <a href="adoptions">Adoptions</a>
            <a href="cafe_transactions">Cafe Transactions</a>
            <a href="product_orders">Product Orders</a>
            <a href="foster_cat_relationships">Foster/Cat Relationships</a>
        </nav>
    </header>
    <main>
        <div class="content-stripe hero-stripe">
            <h1>Manage Foster/Cat Relationships</h1>
        </div>
        <div class="content-stripe table-stripe">
            <table>
            <thead>
                <tr>
                  <!-- Iterate through each key in the first entry to get the
                    column name -->
                    {% for key in foster_cat_relationships[0].keys() %}

                    <!-- Create a <th> tag with the key inside of it, this will be
                    our header row -->
                    <th>{{ key }}</th>

                    <!-- End of this for loop -->
                    {% endfor %}
                </tr>
                <tbody>
        
                <!-- Now, iterate through every relationship in foster cat relationships -->
                {% for foster_cat_relationship in foster_cat_relationships %}
                <tr>
                    <!-- Then iterate through every key in the current person dictionary -->
                    {% for key in foster_cat_relationship.keys() %}

                    <!-- Create a <td> element with the value of that key in it -->
                    <td>{{cat[key]}}</td>
                    {% endfor %}

                    <!-- Create our edit/delete buttons -->
                    <td><a href="edit_foster_cat_relationship/{{foster_cat_relationship.relationship_id}}" class ="edit-del">Edit</a></td>
                    <td><a href="delete_foster_cat_relationship/{{foster_cat_relationship.relationship_id}}" class ="edit-del">Delete</a></td>
                </tr>
                {% endfor %}
                </table>
        
        </div>
        <div class="content-stripe add-stripe">
            <h2>Add Cat</h2>
            <div class="form-region">
                <form id = "insertFosterCatRelationship" action = "/foster_cat_relationships" method = "post">
                    <div class="group-one">
                    <label for="cat-id">Cat ID:</label><br>
                    <input type="int" class = "form-control" id="cat-id" name="cat-id"
                    placeholder = "" required><br>

                    <label for="f-parent-id">Foster Parent ID:</label><br>
                    <input type="int" id="f-parent-id" name="f-parent-id">
                    <div class="group-two">
                    <input class="btn btn-primary" type="submit" value="Submit" name="Add_Foster_Cat_Relationship">
                    <input class="btn" type="button" value="Cancel">
                    </div>
                </form>
            </div>
        </div>
        </main>
        <footer>
            <p>&#169; 2023 Olivia Russell & Jaclyn Rutter</p>
        </footer>
        
</body>
</html>