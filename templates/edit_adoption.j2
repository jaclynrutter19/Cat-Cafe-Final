<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="{{url_for('static', filename='style.css')}}">
    <title>Cat Cafe</title>
</head>
<body>
    <header>
        <p class="company"><a href="/">Cat Cafe</a></p>
        <nav>
             <a href="cats">Cats</a>
            <a href="cafe_products">Cafe Products</a>
            <a href="employees">Employees</a>
            <a href="customers">Customers</a>
            <a href="foster_parents">Foster Parents</a>
            <a href="adoptions">Adoptions</a>
            <a href="cafe_transactions">Cafe Transactions</a>
            <a href="product_orders">Product Orders</a>
            <a href="foster_cat_relationships">Foster/Cat Relationships</a>
        </nav>
    </header>
    <main>
        <div class="content-stripe hero-stripe">
            <h1>Edit Adoption</h1>
        </div>
        <div class="content-stripe table-stripe">
            <table>
            <thead>
                <tr>
                  <!-- Iterate through each key in the first entry to get the
                    column name -->
                    {% for key in adoptions[0].keys() %}

                    <!-- Create a <th> tag with the key inside of it, this will be
                    our header row -->
                    <th>{{ key }}</th>

                    <!-- End of this for loop -->
                    {% endfor %}
                </tr>
                <tbody>
        
                <!-- Now, iterate through every adoption in adoptions -->
                {% for adoption in adoptions %}
                <tr>
                    <!-- Then iterate through every key in the current person dictionary -->
                    {% for key in adoption.keys() %}

                    <!-- Create a <td> element with the value of that key in it -->
                    <td>{{adoption[key]}}</td>
                    {% endfor %}
                </tr>
                {% endfor %}
            </table>

            </div>
        <div class="content-stripe add-stripe">
            <div class="form-region">
                <form id = "editAdoption" action = "/edit_adoption/{{adoptions[0].adoption_id}}" method = "post">
                    <div class="group-one">

                    <label for="customer-id">Customer ID:</label><br>
                    <input type="text" class = "form-control" id="customer-id" name="customer-id" pattern = "[A-Za-z]{1,}"
                    placeholder = "" value = "{{adoptions[0].customer_id}}" required><br>

                    <label for="cat-id">Cat ID:</label><br>
                    <input type="date"  value = "{{adoptions[0].cat_id}}" id="cat-id" name="cat-id">

                    <label for="f-parent-id">Foster Parent ID:</label><br>
                    <input type="int" id="f-parent-id"  value = "{{adoptions[0].foster_parent_id}}"name="f-parent-id">

                    <label for="employee-id">Employee ID:</label><br>
                    <input type="text" id="employee-id"  value = "{{adoptions[0].employee_id}}" name="employee-id">

                    <label for="date">Date:</label><br>
                    <input type="int" id="date"  value = "{{adoptions[0].date}}"name="date">
                    </div>
                    <div class="group-two">
                        <input class="btn" type="submit" value="Save">
                        <input class="btn" type="button" value="Cancel">
                    </div>
                </form>
            </div>
        </div>     
</body>
</html>